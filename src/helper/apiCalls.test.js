import { getPokeTypesData } from './apiCalls';

describe('getPokeTypesData', () => {
  xit('should call fetch with the correct args', () => {});

  xit('should return a resolved promise', () => {});

  xit('should handle a rejected promise', () => {});
});
